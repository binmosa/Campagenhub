{
  "name": "campaignhub-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && VITE_API_BASE_URL=${VITE_API_BASE_URL:-/api} npm --prefix frontend run build",
    "build:backend": "npm --prefix backend ci && npm --prefix backend run build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "deploy:frontend": "bash scripts/deploy-frontend.sh"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
